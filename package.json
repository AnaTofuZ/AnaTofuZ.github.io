{
  "name": "anatofuz-net",
  "version": "1.0.0",
  "scripts": {
    "build": "bun run build.ts",
    "dev": "bun run --hot src/dev.ts",
    "lint": "biome check src/ build.ts",
    "format": "biome check --write src/ build.ts"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.0",
    "@types/bun": "latest"
  }
}
